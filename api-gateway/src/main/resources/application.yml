server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: ride-service
          uri: http://localhost:8083
          predicates:
            - Path=/api/rides/**
        - id: driver-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/drivers/**
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**
        - id: payment-service
          uri: http://localhost:8084
          predicates:
            - Path=/api/payments/**

eureka:
  instance:
    prefer-ip-address: true
    status-page-url: http://localhost:${server.port}/webjars/swagger-ui/index.html
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
